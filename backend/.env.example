# Server Port
PORT=5000

# Database (Leave empty for automatic in-memory MongoDB fallback, or provide local/Atlas URI)
MONGO_URI=

# Authentication Secret
JWT_SECRET=safebridge_jwt_super_secret_key_2026_fob_safety
JWT_EXPIRES_IN=7d

# Anti-Fraud & Verification Configuration
COOLDOWN_HOURS=6
MIN_CROSSING_SECONDS=12
MAX_CROSSING_SECONDS=180
DAILY_CROSSING_CAP=4

# Frontend Client Origin (for CORS)
CLIENT_URL=http://localhost:5173
NODE_ENV=development
